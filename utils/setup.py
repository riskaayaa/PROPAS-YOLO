from setuptools import setup, find_packages

setup(
    name="fakmalpradana_yolo_utils",  # This is the package name you can use in pip
    version="1.1",
    packages=find_packages(),
    install_requires=[],  # Add any dependencies if necessary
)
